info:
  name: Verify Email
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/verify-email"
  body:
    type: json
    data: |-
      {
        "email": "{{testEmail}}",
        "token": "{{verificatinoToken}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
